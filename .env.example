# KATO Environment Configuration
# Copy this file to .env and modify as needed

# API Configuration
KATO_API_PORT=8000
KATO_LOG_LEVEL=INFO

# KATO Processor Manifest (JSON format)
# This contains the processor configuration that would normally be in a genome file
KATO_MANIFEST={"id":"kato-docker","name":"KatoProcessor","indexer_type":"VI","max_sequence_length":0,"persistence":5,"smoothness":3,"auto_act_method":"none","auto_act_threshold":0.8,"always_update_frequencies":false,"max_predictions":100,"recall_threshold":0.1,"quiescence":3,"sort":true,"process_predictions":true}

# MongoDB Configuration
MONGO_DB_PORT=27017

# User configuration (will be set automatically)
USER=${USER}

# Individual processor parameters (alternative to KATO_MANIFEST)
# Uncomment and modify these to override the manifest
# KATO_PROCESSOR_ID=my-processor
# KATO_PROCESSOR_NAME=MyProcessor
# KATO_INDEXER_TYPE=VI
# KATO_MAX_SEQUENCE_LENGTH=0
# KATO_PERSISTENCE=5
# KATO_SMOOTHNESS=3
# KATO_AUTO_ACT_METHOD=none
# KATO_AUTO_ACT_THRESHOLD=0.8
# KATO_ALWAYS_UPDATE_FREQUENCIES=false
# KATO_MAX_PREDICTIONS=100
# KATO_RECALL_THRESHOLD=0.1
# KATO_QUIESCENCE=3
# KATO_SORT=true
# KATO_PROCESS_PREDICTIONS=true