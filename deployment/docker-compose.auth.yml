# Qdrant authentication override
# Generated by kato-manager.sh setup-auth
# Remove with: ./kato-manager.sh disable-auth
services:
  qdrant:
    environment:
      - QDRANT__SERVICE__API_KEY=${QDRANT_API_KEY}
