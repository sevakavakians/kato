pytest>=7.0.0
pytest-timeout>=2.1.0
pytest-xdist>=3.0.0
pytest-asyncio>=0.21.0
requests>=2.28.0
httpx>=0.27.0
# starlette 1.6's test client prefers httpx2 and emits a StarletteDeprecationWarning
# on the httpx fallback. Additive: httpx stays for qdrant-client and for
# tests/tests/fixtures/kato_session_client.py, which imports it directly.
httpx2>=2.0.0
redis>=5.0.1  # match the runtime floor in requirements.txt (aclose())
qdrant-client>=1.7.0
websocket-client>=1.8.0

# GPU testing (optional - requires CUDA-enabled Linux system)
# Install with: pip install cupy-cuda12x  (for CUDA 12.x)
# Or: pip install cupy-cuda11x  (for CUDA 11.x)
# cupy-cuda12x>=12.0.0